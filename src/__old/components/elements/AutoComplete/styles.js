const styles = (theme) => ({
  loading: {
    fontSize: 14,
  },
  option: {
    '&:hover': {
      backgroundColor: theme.color.primary.soft,
    },
    fontSize: 14,
    fontWeight: 400,
    paddingBottom: 8,
    paddingTop: 8,
  },
});

export default styles;
