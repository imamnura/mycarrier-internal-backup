const styles = (theme) => ({
  icon: {
    color: theme.color.general.mid,
  },
  label: {
    color: theme.color.general.mid,
    fontSize: 14,
    letterSpacing: '0.0025em',
  },
});

export default styles;
