const styles = (theme) => ({
  page: {
    background: theme.color.general.soft,
    padding: '8px 0px',
  },
  pageContainer: {
    minHeight: '40vh',
    padding: '2px 0px',
    width: '100%',
  },
  pageRoot: {
    maxHeight: '70vh',
    minHeight: '40vh',
    overflowY: 'auto',
    padding: '0px 8px',
    width: '100%',
  },
});

export default styles;
