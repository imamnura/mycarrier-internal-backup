const styles = (theme) => ({
  bottomDivider: {
    backgroundColor: theme.color.general.soft,
    height: 1,
    marginTop: -1,
    width: '100%',
  },
  filterContainer: {
    alignItems: 'center',
    display: 'flex',
    justifyContent: 'space-between',
    padding: '8px 0px',
  },
});

export default styles;
