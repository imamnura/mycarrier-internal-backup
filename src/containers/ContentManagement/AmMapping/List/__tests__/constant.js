import { tableHeader } from '../constant';

describe('src/containers/ContentManagement/AmMapping/List/constant', () => {
  it('tableHeader test not null', () => {
    expect(tableHeader).not.toBeNull();
  });
});
