import { tableHeader } from '../constant';

describe('src/containers/ContentManagement/Product/List-v2/constant', () => {
  test('tableHeader', () => {
    expect(tableHeader).not.toBeNull();
  });
});
