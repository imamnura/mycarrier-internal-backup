import { validation } from '../yupResolver';

describe('src/pages/ContentManagement/Events/Add-v2/lib/sections/Speakers/yupResolver', () => {
  test('validation', () => expect(validation).not.toBeNull());
});
