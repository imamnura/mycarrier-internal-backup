import { validation } from '../yupResolver';

describe('src/pages/ContentManagement/Events/Add-v2/lib/sections/RelatedProduct/yupResolver', () => {
  test('validation', () => expect(validation).not.toBeNull());
});
