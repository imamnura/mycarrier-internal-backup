import { validation } from '../yupResolver';

describe('src/pages/ContentManagement/Events/Add-v2/lib/sections/Attendees/yupResolver', () => {
  test('validation', () => expect(validation).not.toBeNull());
});
