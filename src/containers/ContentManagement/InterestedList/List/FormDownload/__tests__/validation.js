import validation from '../validation';

describe('src/pages/ContentManagement/InterestedList/List/FormDowanload/validation', () => {
  test('validation', () => expect(validation).not.toBeNull());
});
