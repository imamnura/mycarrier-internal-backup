import styles from '../styles';
// import theme from '@styles/theme';

describe('src/pages/Microsite/ApprovalBakes/element/ListReviewer/styles', () => {
  test('styles', () => {
    // expect(styles(theme)).not.toBeNull();
    expect(styles()).not.toBeNull();
  });
});
