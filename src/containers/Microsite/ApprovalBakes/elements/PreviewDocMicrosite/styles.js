const styles = (theme) => ({
  page: {
    background: theme.color.general.soft,
    display: 'flex',
    justifyContent: 'center',
    padding: '8px 0px',
    width: '100%',
  },
});

export default styles;
