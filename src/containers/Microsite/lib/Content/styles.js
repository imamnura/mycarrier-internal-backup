const styles = (theme) => ({
  content: {
    border: `1px solid ${theme.color.general.soft}`,
    borderRadius: 10,
    padding: 16,
    overflowX: 'auto',
  },
  logo: {
    border: `1px solid ${theme.color.general.soft}`,
    borderRadius: 5,
    padding: 8,
    width: 50,
  },
  mainWrapper: {
    backgroundColor: 'white',
    border: `1px solid ${theme.color.general.soft}`,
    borderRadius: 10,
    padding: 16,
  },
});

export default styles;
