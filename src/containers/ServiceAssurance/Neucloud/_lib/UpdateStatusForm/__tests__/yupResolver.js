import { validation } from '../yupResolver';

describe('src/pages/ServiceAssurance/Neucloud/Detail/lib/UpdateStatusForm/yupResolver', () => {
  it('yupResolver', () => {
    expect(validation).not.toBeNull();
  });
});
