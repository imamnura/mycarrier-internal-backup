import { validation1 } from '../yupResolver';

describe('src/containers/ServiceAssurance/Gameqoo/Validation/yupResolver', () => {
  it('yupResolver', () => {
    expect(validation1).not.toBeNull();
  });
});
