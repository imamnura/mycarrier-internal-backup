import validation from '../validation';

describe('src/containers/ServiceAssurance/Gameqoo/Detail/lib/forms/ApproveIssueForm/validation', () => {
  it('yupResolver', () => {
    expect(validation).not.toBeNull();
  });
});
