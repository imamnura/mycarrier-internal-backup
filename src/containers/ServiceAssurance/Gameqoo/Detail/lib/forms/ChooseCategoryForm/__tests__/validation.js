import validation from '../validation';

describe('src/containers/ServiceAssurance/Gameqoo/Detail/lib/forms/ChooseCategoryForm/validation', () => {
  it('yupResolver', () => {
    expect(validation).not.toBeNull();
  });
});
