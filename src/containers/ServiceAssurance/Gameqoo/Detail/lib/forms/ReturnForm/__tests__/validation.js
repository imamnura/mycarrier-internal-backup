import { validation } from '../validation';

describe('src/containers/ServiceAssurance/Gameqoo/Detail/lib/forms/ReturnForm/validation', () => {
  it('yupResolver', () => {
    expect(validation).not.toBeNull();
  });
});
