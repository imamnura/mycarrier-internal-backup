import { validation } from '../validate';

describe('src/containers/ServiceAssurance/DigitalProduct/Detail/elements/TicketNumber/validate', () => {
  test('validation', () => expect(validation).not.toBeNull());
});
