import validation from '../validation';

describe('src/containers/ServiceAssurance/DigitalProduct/Detail/elements/ChooseCategoryForm/validation', () => {
  it('yupResolver', () => {
    expect(validation).not.toBeNull();
  });
});
