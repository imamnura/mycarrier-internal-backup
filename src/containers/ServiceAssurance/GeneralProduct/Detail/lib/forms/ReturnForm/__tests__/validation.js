import { validation } from '../validation';

describe('src/containers/ServiceAssurance/GeneralProduct/Detail/lib/ReturnForm/yupResolver', () => {
  it('yupResolver', () => {
    expect(validation).not.toBeNull();
  });
});
