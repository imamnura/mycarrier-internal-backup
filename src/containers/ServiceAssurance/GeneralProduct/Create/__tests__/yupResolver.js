import { validation1 } from '../yupResolver';

describe('src/containers/ServiceAssurance/GeneralProduct/Create/yupResolver', () => {
  it('yupResolver', () => {
    expect(validation1).not.toBeNull();
  });
});
