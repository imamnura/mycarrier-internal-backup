import { validation } from '../yupResolver';

describe('src/pages/ServiceAssurance/GeneralProduct/List/lib/RejectForm/yupResolver', () => {
  it('yupResolver', () => {
    expect(validation).not.toBeNull();
  });
});
