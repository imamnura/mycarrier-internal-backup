import Chip from '@components/Chip';

const chipOverview = {
  component: Chip,
  variant: [
    {
      name: 'Default',
      props: {
        label: 'Chip Label',
      },
    },
  ],
};

export default chipOverview;
