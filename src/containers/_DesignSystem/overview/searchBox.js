import SearchBox from '@components/SearchBox';

const searchBoxOverview = {
  component: SearchBox,
  variant: [
    {
      name: 'Default',
      props: {
        placeholder: 'Placeholder',
      },
    },
  ],
};

export default searchBoxOverview;
